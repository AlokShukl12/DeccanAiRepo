[project]
name = "talent-scouting-agent"
version = "0.1.0"
description = "AI-powered talent scouting and engagement agent."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.117.1",
    "pandas>=2.0.0",
    "streamlit>=1.35.0",
]

[project.scripts]
app = "server:app"
